{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "composer install --no-interaction --prefer-dist --optimize-autoloader --no-dev && NPM_CONFIG_PRODUCTION=false npm ci --include=dev --no-audit --no-fund && npm run build"
  },
  "deploy": {
    "startCommand": "sh docker/start.sh",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
