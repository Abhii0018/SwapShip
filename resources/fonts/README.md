Place your font files here (woff2 preferred):

- ClashGrotesk.woff2 (for `Clash Grotesk` display headings)
- GeneralSans.woff2 (for body / UI text)

Name the files exactly as above or update `resources/css/season04.css` to match your filenames.

Note: These are placeholders — replace with licensed font files before production.
